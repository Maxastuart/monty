#include "monty.h"

/**
 * nop - does nothing
 * @stack: unused.
 * @line_number: unused.
 *
 * Return: always 0.
 */
int nop(stack_t **stack __attribute__((unused)),
	unsigned int line_number __attribute__((unused)))
{
	return (0);
}
